#!/bin/bash
HOST=$(hostname)
DATE=$(date)
KERNEL=$(uname -r)
echo "This is for testing"
echo "======================================================================================="
echo "$HOST"
echo "$DATE"
echo "$KERNEL"
